import React from 'react';
import { Router, Route, hashHistory } from 'react-router';

import App from './app';

const Root = () => {
  return <h1>Hello</h1>;
};

export default Root;

// return (
//   <Router history={hashHistory}>
//
//   </Router>
// );
